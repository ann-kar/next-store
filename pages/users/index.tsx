const UsersIndex = () => {

    return (
        <div>
            <h1>directory &quot;users&quot;, index page</h1>
            <p>unavailable because &quot;users.tsx&quot; from the main directory is displayed instead</p>
        </div>
    )
}

export default UsersIndex