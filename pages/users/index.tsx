const UsersIndex = () => {

    return (
        <div>
            <h1>directory &quot;users&quot;, index page</h1>
            <p>
                displayed by vercel but not in the version that runs locally
                (&quot;users.tsx&quot; from the main directory is displayed instead)
            </p>
        </div>
    )
}

export default UsersIndex